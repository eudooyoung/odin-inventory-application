<header>
  <h1><%= title %></h1>
  <nav>
    <ul style="display: flex; gap: 2rem; list-style-type: none">
      <% links.forEach(link => { %>
      <li>
        <a href="<%= link.href %>"><%= link.text %></a>
      </li>
      <% }) %>
    </ul>
  </nav>
</header>
